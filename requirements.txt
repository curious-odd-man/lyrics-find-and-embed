glob2
requests>=2.9.1
mutagen>=1.31
beautifulsoup4>=4.4.1
win-unicode-console>=0.4; sys_platform == 'win32'


### For animate album art
# Core
torch>=2.2.0            # install the CUDA build matching your driver, see README
diffusers>=0.27.0
transformers>=4.38.0
accelerate>=0.27.0
safetensors>=0.4.2

# Tag / album art extraction
mutagen>=1.47.0

# Image / video handling
Pillow>=10.0.0
imageio>=2.34.0
imageio-ffmpeg>=0.4.9    # bundles an ffmpeg binary, no system install required

# Nice-to-have
tqdm>=4.66.0
